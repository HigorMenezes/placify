import { makeStyles } from "@material-ui/styles";

const usePlacifyTitleStyles = makeStyles({
  root: {
    fontFamily: "Signika",
    fontSize: 42,
    lineHeight: 1,
    color: "#5A5A5A",
    textShadow: "0px 4px 4px rgba(0, 0, 0, 0.25)",
  },
});

export default usePlacifyTitleStyles;
